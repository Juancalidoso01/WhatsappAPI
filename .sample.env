# ===== WhatsApp / Meta App Configuration =====
ACCESS_TOKEN=ADD_APP_TOKEN_HERE
APP_SECRET=ADD_APP_SECRET_HERE
APP_ID=ADD_APP_ID_HERE
PHONE_NUMBER_ID=ADD_PHONE_NUMBER_ID_HERE
WABA_ID=ADD_WABA_ID_HERE
VERIFY_TOKEN=ADD_VERIFY_TOKEN_HERE

# ===== Integration API (external systems / CRM) =====
INTEGRATION_API_KEY=

# FAQ Centro de Ayuda (base de conocimiento del agente IA)
FAQ_SITE_URL=https://faq-sooty-theta.vercel.app

# Gemini (misma key que FAQ Punto Pago / Google AI Studio)
GOOGLE_GENERATIVE_AI_API_KEY=
# GEMINI_MODEL=gemini-2.5-flash

# ===== Dashboard security =====
# Opción A: Google OAuth (gratis) — solo emails del dominio indicado
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# Por defecto: puntopago.net (sin @)
ALLOWED_EMAIL_DOMAIN=puntopago.net

# Opción B: contraseña compartida (cookie de sesión 7 días). Puede coexistir con Google.
DASHBOARD_PASSWORD=
# Firma de sesión OAuth (recomendado: APP_SECRET en producción)
# DASHBOARD_SECRET=

# Cron de cargas masivas: un servicio externo (o el panel con sesión) llama cada 1–5 min:
#   POST https://tu-dominio.vercel.app/api/campaigns/cron/tick
#   Header: Authorization: Bearer <CRON_SECRET>
# Nota: Vercel Hobby no permite cron cada minuto en vercel.json; usa cron-job.org, Upstash QStash, etc.
CRON_SECRET=

# Solo en pruebas: permite POST /api/simulate-incoming en producción
# ALLOW_SIMULATE=true

# ===== Server Configuration =====
PORT=8080
# URL pública HTTPS (requerida para WhatsApp Flows con endpoint dinámico)
PUBLIC_BASE_URL=https://whatsapp-api-ten-tau.vercel.app
# Opcional: URL pública de imagen de tarjeta (si no, usa /assets/punto-pago-card.png)
# CARD_IMAGE_URL=

# ===== Redis (only used locally) =====
REDIS_HOST=localhost
REDIS_PORT=6379
